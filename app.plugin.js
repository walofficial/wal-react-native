// Legacy local config plugin entrypoint.
// The project now uses `@bacons/apple-targets` directly via `app.config.js` plugins.
// Keep this file as a harmless no-op for older tooling references.
module.exports = (config) => config;
